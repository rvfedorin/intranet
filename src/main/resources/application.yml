server:
  port: 8080
spring:
  datasource:
    url: jdbc:h2:file:./db/operations-h2-db
    username:
    password:
  jpa:
    show-sql: true
    properties:
      hibernate.format_sql: true
    hibernate:
      ddl-auto: update

  h2:
    console.enabled: true
